service: {{name}}

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs10.x

functions:
  {{lib}}:
    handler: dist/index.{{lib}}
    events:
      - http:
          path: /
          method: ANY
      - http:
          path: /{any+}
          method: ANY
