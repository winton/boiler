import GenerateSchema from "generate-schema"
import { {{table}} } from "./{{table}}"

export class {{class}} {
  {{table}}(): Record<string, string>[] {
    return GenerateSchema.bigquery({{table}})
  }
}
