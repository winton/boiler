import expect from "./expect"
import { {{class}} } from "../src"

describe("{{lib}}", () => {
  it("should instantiate", () => {
    new {{class}}()
  })

  it("should assert", () => {
    expect(true).toBe(true)
  })
})
