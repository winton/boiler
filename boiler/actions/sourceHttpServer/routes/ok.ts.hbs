import http from "http"
import pino from "pino"

export class OkRoute {
  match(req: http.IncomingMessage): boolean {
    return req.url === "/ok"
  }

  async route(req: http.IncomingMessage): Promise<void> {}
}
